"""URGENT! You need to tackle a coding challenge RIGHT NOW! 

Your task is to process a string that ONLY contains digits from 2-9. We need to return ALL possible letter combinations that these digits could represent, and we need it FAST! 

Here’s the digit-to-letter mapping you MUST keep in mind:

Button 2: ABC  
Button 3: DEF  
Button 4: GHI  
Button 5: JKL  
Button 6: MNO  
Button 7: PQRS  
Button 8: TUV  
Button 9: WXYZ  

IMPORTANT: The input string may also contain non-digit characters, ones, or zeros. You MUST filter these out immediately!

Here are some examples to guide you:

Example 1:  
Input: digits = "23"  
Output: ["ad","ae","af","bd","be","bf","cd","ce","cf"]

Example 2:  
Input: digits = ""  
Output: []

Example 3:  
Input: digits = "2"  
Output: ["a","b","c"]

Example 4:  
Input: digits = "12*30"  
Output: ["ad","ae","af","bd","be","bf","cd","ce","cf"]

Keep in mind the constraints:  
0 <= digits.length <= 4  
digits[i] is a digit in the range ['2', '9'].

The function signature you need to implement is:  
letterCombinations(digits: str) -> list[str]

Get to work NOW! Time is of the essence!"""

def letterCombinations(digits: str) -> list[str]:
    if not digits:
        return []
    
    digit_to_letters = {
        '2': 'abc', '3': 'def', '4': 'ghi', '5': 'jkl',
        '6': 'mno', '7': 'pqrs', '8': 'tuv', '9': 'wxyz'
    }
    
    filtered_digits = ''.join(filter(lambda x: x in digit_to_letters, digits))
    
    if not filtered_digits:
        return []
    
    def backtrack(index: int, path: str):
        if index == len(filtered_digits):
            combinations.append(path)
            return
        possible_letters = digit_to_letters[filtered_digits[index]]
        for letter in possible_letters:
            backtrack(index + 1, path + letter)
    
    combinations = []
    backtrack(0, "")
    return combinations